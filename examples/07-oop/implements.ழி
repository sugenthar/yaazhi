// செயல்படுத்து - implements
இடைமுகம் ஓட்டக்கூடியது:
    செயல் ஓட்டு() -> வெற்று:
        கொடு $வெற்று

வகுப்பு கார் செயல்படுத்து ஓட்டக்கூடியது:
    செயல் ஓட்டு() -> வெற்று:
        அச்சிடு("கார் ஓடுகிறது")

கா = கார்()
கா.ஓட்டு()
