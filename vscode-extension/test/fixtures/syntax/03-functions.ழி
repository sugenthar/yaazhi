// செயல் அறிவிப்புகள் — அளவுருக்கள், வகை, திரும்பல், இயல்புநிலை
செயல் வணக்கம்():
    அச்சிடு("வணக்கம்")

செயல் கூட்டல்(அ : எண், ஆ : எண்) -> எண்:
    கொடு அ + ஆ

செயல் பெயரிடு(பெயர் : சொல் = "யாழி") -> சொல்:
    கொடு பெயர்

செயல் முழு_கூட்டல்(அ : எண், ஆ : எண் = $௧) -> எண்:
    கொடு அ + ஆ

செயல் பொது_சரிபார்() -> எதுவும்:
    கொடு $வெற்று

// பொது செயல் உள்-வகுப்பு அல்ல — தனி மட்ட வகுப்பில் மட்டுமே
