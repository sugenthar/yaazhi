// பல்லுருவாக்கம்
வகுப்பு வடிவம்:
    செயல் பரப்பு() -> எண்:
        கொடு $௦

வகுப்பு சதுரம் சேர்தல் வடிவம்:
    பக்கம்: எண் = $௦
    செயல் உருவாக்கு(பக்கம்: எண்):
        இது.பக்கம் = பக்கம்
    செயல் பரப்பு() -> எண்:
        கொடு இது.பக்கம் * இது.பக்கம்

ச = சதுரம்($௫)
அச்சிடு(ச.பரப்பு())
